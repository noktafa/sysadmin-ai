apiVersion: v1
kind: Secret
metadata:
  name: sysadmin-ai-secrets
  labels:
    app: sysadmin-ai
type: Opaque
stringData:
  # Replace with your actual API key
  SYSADMIN_AI_API_KEY: "REPLACE_ME"
  # Uncomment and set if using OpenAI provider
  # OPENAI_API_KEY: "REPLACE_ME"
